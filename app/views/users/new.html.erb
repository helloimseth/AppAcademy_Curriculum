<%= render "sessions/errors", user: @user %>

<%= flash[:notice] unless flash[:notice].nil? %>

<h1>Sign Up</h1>
<form action="<%= users_url %>" method="post">
  <%= auth_token %>

  <%= email_password_form %>
</form>
