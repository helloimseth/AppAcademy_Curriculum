<%= render "errors", album: @album %>

<h1>Add Album</h1>
<form action="<%= albums_url  %>" method="post">
  <%= auth_token %>

  <%= render "form", album: @album  %>

  <%= submit_button %>
</form>

<form action="<%= band_url(params[:band_id].to_i) %>" method="get">
  <button>Cancel</button>
</form>
